### Conceptual Exercise

Answer the following questions below:

- What is PostgreSQL? : a type of database 

- What is the difference between SQL and PostgreSQL? : postgres is a database type , sql is a data base language

- In `psql`, how do you connect to a database? : type spql , \c NAME_OF_DB 

- What is the difference between `HAVING` and `WHERE`? : i donot understand the question 

- What is the difference between an `INNER` and `OUTER` join? : inner join combines rows that have things in common from several tables  , outer joins all rows from several tables 

- What is the difference between a `LEFT OUTER` and `RIGHT OUTER` join? :  Left Outer Join retrieves all the rows from both the tables that satisfy the join condition along with the unmatched rows of the left table.

Right Outer Join retrieves all the rows from both the tables that satisfy the join condition along with the unmatched rows of the right table.



- What is an ORM? What do they do? Object relational mapping  they are used to combine object oriented programing and databases 

- What are some differences between making HTTP requests using AJAX 
- and from the server side using a library like `requests`? : the client doesnt see any request made from the server but can see all requests made by the client in AJAX , so any requests that needs to be hidden from the user needs to be done on the back end 

- What is CSRF? What is the purpose of the CSRF token? : a way to make sure forms that are submitted to your server come from a trusted form and not a malicous actor trying to pass unknow data to our server 

- What is the purpose of `form.hidden_tag()`? : to hide things like our csrf token from the user 
